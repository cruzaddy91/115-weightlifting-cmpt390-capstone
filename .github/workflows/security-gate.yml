name: Security Gate

on:
  push:
  pull_request:

jobs:
  secret-scan:
    name: Secret and sensitive-data scan
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run repo security gate
        run: python3 scripts/security_gate.py --mode tracked
